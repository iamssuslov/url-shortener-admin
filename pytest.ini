[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
markers =
  api: API tests
  ui: UI tests
  smoke: Smoke tests
  regression: Regression tests
  integration: Integration tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function